{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "jeskojets.com"
      }
    }
  ],
  "routes": [
    {
      "src": "/profile-cards/(.*)",
      "dest": "/profile-cards/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
